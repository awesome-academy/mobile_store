<%= form_for @comment, remote: true do |f| %>
	<div class="field">
		<%= f.text_area :content, placeholder: "Compose new comment..." %>
		<%= f.hidden_field :product_id, value: @comment.product_id %>
	</div>
	
	<%= f.submit "Post", class: " btn-primary btn-sm btn-submit" %>
<% end %>
